#include <bits/stdc++.h>

using namespace std;

typedef long long C;
typedef complex<C> P;
#define X real()
#define Y imag()

P points[2010];

int main() {
  int a, b, n;
  cin >> a >> b >> n;
  P O = {a, b};
  for(int i = 0; i < n; i++) {
    cin >> a >> b;
    points[i] = {a, b};
  }
  
}
